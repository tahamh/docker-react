version: '3.9'

services:
   web:
    build:
     context: .
     dockerfile: Dockerfile.dev
    ports: 
      - "3000:3000" 
    volumes: 
      - C:\Users\TAHA\documents\frontend:/app
      - /app/node_modules
           
